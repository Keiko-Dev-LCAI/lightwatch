# LightView Scanner Service

Download both files to your Desktop, then:

1. Open Command Prompt and run once:
   pip install pillow flask flask-cors
2. Double-click start-scanner.bat each time before using Scan Ticket in LightView
3. Keep the black window open while scanning
